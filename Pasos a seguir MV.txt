Para instalar el ssh --> sudo apt-get install openssh-server

Para las Guest Additions --> mount /dev/sr0 /mnt
			     sudo apt-get install build-essential module-assistant
			     sudo sh /mnt/VBoxLinuxAdditions.run

Para la carpeta compartida (de nombre igual en localhost y MV) --> Configuracion > Carpetas compartidas > Agregar la del localhost con la opción de permanente.
								   mount -t vboxfs <nombrecarpeta> -o uid=1000,gid=1000 /home/usuarioMV/nombrecarpeta
								  
Para la instalación de Rails --> http://blog.sudobits.com/2012/05/02/how-to-install-ruby-on-rails-in-ubuntu-12-04-lts/
				 De esa página ir al enlace de Git > Raw > Copiar URL
				 En la MV wget URL
				 ./rails-installer.sh
				 source ~/.rvm/scripts/rvm

Arrancar el servidor Ruby --> rails new nombreproyecto cd nombrecarpeta
			      source ~/.rvm/scripts/rvm
			      Dentro de la carpeta > rails server
			      En un navegador fuera de la MV > localhost:3000

Para instalar Django --> https://www.djangoproject.com/download/
			 Option 1 > Descargar el tar.gz
			 tar xzvf Django-1.4.5.tar.gz
			 cd Djando-1.4.5
			 sudo python setup.py install

Para comprobar que está instalado Python --> python
					     >>import django
					     >>print django.get_version()
						Muestra 1.4.5

Tutorial Django --> https://docs.djangoproject.com/en/1.4/intro/tutorial01/
		    Crear una carpeta dentro de la compartida para esto.
		    Al último comando del tutorial agregarle 0.0.0.0:8000
					     

			      